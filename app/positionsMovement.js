angular.module("app2048").service("positionsMovement", function () {
    //todo passar métodos de manipulação para esse service, melhorar o retorno do position para usar as deps corretamente
    this.test = (arg) => console.log('test: ', arg)
})